apiVersion: v1
kind: Service
metadata:
  name: reorg-monitor
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app.kubernetes.io/name: reorg-monitor
  ports:
    - name: webserver
      port: 9090
      targetPort: webserver